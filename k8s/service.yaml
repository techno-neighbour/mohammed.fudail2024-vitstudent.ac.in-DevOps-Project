apiVersion: v1
kind: Service

metadata:
  name: portfolio

spec:
  type: NodePort

  selector:
    app: portfolio

  ports:
    - name: app
      port: 8083
      targetPort: 8083
      nodePort: 30083

    - name: actuator
      port: 9090
      targetPort: 9090
      nodePort: 30090